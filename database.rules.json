{
  "rules": {
    "admins": {
      ".read": "auth != null",
      ".write": "auth != null && (root.child('admins').child(auth.uid).val() === true || auth.token.email === 'admin@roomie.io' || auth.token.admin === true)"
    },
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        ".write": "auth != null && (auth.uid === $uid || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        "friends": {
          "$friendId": {
            ".write": "auth != null && (auth.uid === $uid || auth.uid === $friendId || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)"
          }
        }
      }
    },
    "private_chats": {
      "$chatId": {
        ".read": "auth != null && $chatId.contains(auth.uid)",
        ".write": "auth != null && $chatId.contains(auth.uid)"
      }
    },
    "bookmarks": {
      "$userKey": {
        ".read": "auth != null && (auth.uid === $userKey || (auth.token.email != null && auth.token.email.split('@')[0] === $userKey.split('_')[0]) || $userKey === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        ".write": "auth != null && (auth.uid === $userKey || (auth.token.email != null && auth.token.email.split('@')[0] === $userKey.split('_')[0]) || $userKey === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)"
      }
    },
    "shared_notes": {
      ".read": "auth != null",
      "$noteId": {
        ".write": "auth != null && (!data.exists() || data.child('authorUid').val() === auth.uid || data.child('authorEmail').val() === auth.token.email || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)"
      }
    },
    "note_shares": {
      "$userKey": {
        ".read": "auth != null && (auth.uid === $userKey || (auth.token.email != null && auth.token.email.split('@')[0] === $userKey.split('_')[0]) || $userKey === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        "$shareId": {
          ".write": "auth != null"
        }
      }
    },
    "friend_requests": {
      ".read": "auth != null",
      "$requestId": {
        ".write": "auth != null && (!data.exists() || data.child('senderUid').val() === auth.uid || data.child('receiverUid').val() === auth.uid || newData.child('senderUid').val() === auth.uid)"
      }
    },
    "notifications": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || $uid === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        ".write": "auth != null"
      }
    },
    "custom_communities": {
      ".read": "auth != null",
      "$communityId": {
        ".write": "auth != null && (!data.exists() || data.child('createdBy').val() === auth.token.email || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)",
        "channels": {
          ".write": "auth != null",
          "$channelId": {
            "messages": {
              ".write": "auth != null"
            },
            "typing": {
              ".write": "auth != null"
            }
          }
        }
      }
    },
    "pdf_contents": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "community_users": {
      ".read": "auth != null",
      "$userKey": {
        ".write": "auth != null && (auth.uid === $userKey || (auth.token.email != null && auth.token.email.split('@')[0] === $userKey.split('_')[0]) || $userKey === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true)"
      }
    },
    "community_channels": {
      ".read": "auth != null",
      "$channelId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true || !data.exists())",
        "messages": {
          ".write": "auth != null"
        },
        "typing": {
          ".write": "auth != null"
        },
        "notes": {
          ".write": "auth != null"
        },
        "pdfs": {
          ".write": "auth != null"
        },
        "metadata": {
          ".write": "auth != null"
        }
      }
    },
    "community_groups": {
      ".read": "auth != null",
      "$groupId": {
        ".write": "auth != null && (auth.token.admin === true || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true || !data.exists() || data.child('metadata/createdBy').val() === auth.token.email || newData.child('metadata/createdBy').val() === auth.token.email)",
        "members": {
          "$userSlug": {
            ".write": "auth != null"
          }
        },
        "joinRequests": {
          "$userSlug": {
            ".read": "auth != null",
            ".write": "auth != null && (newData.child('email').val() === auth.token.email || data.child('email').val() === auth.token.email || data.parent().parent().child('metadata/createdBy').val() === auth.token.email || auth.token.email === 'admin@roomie.io' || root.child('users').child(auth.uid).child('isAdmin').val() === true || auth.token.admin === true)"
          }
        },
        "messages": {
          ".write": "auth != null"
        },
        "notes": {
          ".write": "auth != null"
        },
        "pdfs": {
          ".write": "auth != null"
        },
        "roadmap": {
          ".write": "auth != null"
        },
        "tasks": {
          ".write": "auth != null"
        },
        "announcements": {
          ".write": "auth != null"
        },
        "metadata": {
          ".write": "auth != null"
        }
      }
    },
    "study_rooms": {
      ".read": "auth != null",
      "$roomId": {
        ".write": "auth != null && (auth.token.admin === true || !data.exists() || data.child('hostEmail').val() === auth.token.email || data.child('hostPeerId').val() === auth.uid)",
        "participants": {
          "$peerId": {
            ".write": "auth != null && (newData.child('uid').val() === auth.uid || data.child('uid').val() === auth.uid || newData.child('email').val() === auth.token.email || data.child('email').val() === auth.token.email || data.parent().parent().child('hostEmail').val() === auth.token.email || auth.token.admin === true)"
          }
        },
        "joinRequests": {
          "$peerId": {
            ".write": "auth != null && (newData.child('uid').val() === auth.uid || data.child('uid').val() === auth.uid || newData.child('email').val() === auth.token.email || data.child('email').val() === auth.token.email || data.parent().parent().child('hostEmail').val() === auth.token.email || auth.token.admin === true)"
          }
        },
        "messages": {
          ".write": "auth != null"
        },
        "calls": {
          "$callId": {
            ".write": "auth != null"
          }
        },
        "notes": {
          ".write": "auth != null"
        },
        "pdfs": {
          ".write": "auth != null"
        }
      }
    },
    "latency_test": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "leaderboard": {
      ".read": "auth != null",
      "$userKey": {
        ".write": "auth != null && (auth.uid === $userKey || (auth.token.email != null && auth.token.email.split('@')[0] === $userKey.split('_')[0]) || $userKey === 'guest_' + auth.uid + '@roomie_io' || auth.token.admin === true)"
      }
    }
  }
}
